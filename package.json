{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "start-json-server": "json-server db.json --watch --port 8000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
